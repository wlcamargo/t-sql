--INSERIR UM CSV NO BANCO DE DADOS

--CRIAR TABELA
CREATE TABLE TB_PAIS(
	PAIS VARCHAR(20),
	SIGLA VARCHAR(20)
)
GO


BULK INSERT
TB_PAIS FROM 'caminho_do_arquivo.csv'
WITH
(
	FIRSTROW = 2,
	FIELDTERMINATOR = ';',
	ROWTERMINATOR = '\n'
)

SELECT * FROM TB_PAIS